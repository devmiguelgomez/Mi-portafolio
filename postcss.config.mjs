// Actualizar este archivo
const config = {
  plugins: {
    'tailwindcss/nesting': {},
    tailwindcss: {},
    autoprefixer: {}
    // Removido cssnano para evitar errores de dependencia
  },
};

export default config;
